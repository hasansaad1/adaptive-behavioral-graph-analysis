# Reproduce Chapter C

From the repository root, with the project virtualenv:

```bash
.venv/bin/python -m abrg.chapter_c
```

This regenerates every number under `abrg/output/v2_chapter_c/` (SUMMARY.md, artefacts, tensors, figures).
